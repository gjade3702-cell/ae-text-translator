# Changelog

## [2.0.17] — 2026-08-20

### Bug 修复（代码审查 + 单元测试驱动）
- 修复重试次数 / 请求间隔设为 0 后，重新打开被 `||` 改回默认值（2 / 400）的持久化 bug——改用 `isNaN` 判断，0 可被正确保留
- 修复 Base64 解码在长 Key 下可能因 32 位位运算溢出而失真——解码后清理已消费高位，并用 `>>> 0` 防负数
- curl 增加 `-L` 跟随重定向（短链 / http→https 跳转不再误判为失败）
- HTTP 状态码判断由「只认 200」改为「2xx 都算成功」
- 修复目标语言查重跳过某语言时进度条瞬时过计（复位 layerIdx 与 curNewLayers）
- JSON 转义补充垂直制表符 \x0B；字符串提取对末端孤立反斜杠做越界保护
- API Key 掩码逻辑统一：短 Key 显示首 1 尾 1，长 Key 显示首 4 尾 4
- 清理一处重复注释

## [2.0.16] — 2026-08-20

### 主界面拉伸适配
- 窗口尺寸限制在合理范围（最小 430×400，最大 640×600），避免拉到极端导致布局崩坏（部分 AE 版本 maximumSize 不可靠，改为 onResize 时强制夹紧）
- onResize 改为完整重排（layout(true)+resize）替代仅 resize()，拉伸时控件正确跟随，不再错位/重叠

## [2.0.15] — 2026-08-20

### 提示方式
- 「多个目标语言用逗号隔开」改为**鼠标悬停提示**（tooltip 样式，悬停在目标语言输入框上显示），不再占用一行的常驻小字

### 精简
- 移除「自定义语言」设置模块（用处不大）；「名字=代码」在输入框内联的写法仍保留可用

## [2.0.14] — 2026-08-20

### 预合成与图层名
- 「自动预合成」默认改为**关闭**（三个选项：查重 / 自动预合成 / 按行翻译 均保持记忆，记住上次勾选状态）
- 关闭自动预合成时：翻译完成后把副本图层名**清空**，AE 会以图层文字内容作为显示名（与源文字一致），不再沿用母本名

## [2.0.13] — 2026-08-20

### 目标语言
- 默认打开时目标语言输入框为空；第二次打开恢复上一次输入的内容（记忆功能不变）
- 输入框下方常驻提示简化为「支持: 中文名「英语」 / 代码「EN」」
- 新增焦点提示：点击/停顿在输入框时显示「多个目标语言用逗号或顿号隔开，如: 英语, 法语」，失焦自动隐藏

### 自定义语言管理
- 「名字=代码」的自定义语言从输入框内联改到设置对话框独立管理：设置 → 自定义语言（每行「名字=代码」，可自由增删）
- 自定义条目优先于内置 44 语言字典；输入框内联的「名字=代码」写法仍兼容

## [2.0.12] — 2026-08-20

### API Key 安全
- Key 存储改为 XOR + Base64 混淆（`ds1:` 前缀），不再是纯明文躺在 prefs 文件里；旧版本明文 Key 自动兼容读取并在下次保存时转为混淆存储
- 设置对话框中的 Key 只显示掩码（开头 4 位 + ⚫ + 结尾 4 位）；点「修改」才打开输入框，「清除」可移除
- 说明：ExtendScript 无加密库，这是防随手可见的混淆而非银行级加密，已写入 README
- 修复合手写的 Base64 编码 bug（第三组取位错误导致部分 Key 还原出错），新增 11 项单元测试覆盖混淆/掩码

## [2.0.11] — 2026-08-20

### UI 精简
- 移除「取消」按钮（同步执行模式下无法中途取消，按钮无实际作用）
- 窗口 / 面板标题由「DeepSeek 翻译器 v2.0.10」改为「AE 翻译助手 / AE Translator」（脚本已支持任意 OpenAI / Anthropic 兼容接口，不再局限于 DeepSeek；标题不再显示版本号）

## [2.0.10] — 2026-08-20

### UI 精简
- 提示词（Prompt）编辑区移入设置对话框（设置 → 提示词），主界面不再占用大块空间，窗口默认尺寸相应缩小（440×420）
- 「恢复默认提示词」按钮同步移入设置对话框

### 目标语言记忆加固
- 目标语言输入框增加「输入即存」（onChanging），即使没有失焦/点开始处理，输入的内容也会立即保存到 AE 设置；下次打开面板恢复上次输入
- 原 onChange / 开始处理 / 关闭面板 三个保存时机保留

## [2.0.9] — 2026-08-20

### UI 精简
- 移除主界面底部的「重试失败项」按钮及其逻辑（失败项可通过导出日志留档）
- 「导出日志」移入设置对话框（设置 → 日志行），主界面底部不再有结果按钮行
- 导出无日志时给出明确提示

## [2.0.8] — 2026-08-20

### 修复严重 bug：翻译作用在母本而非副本上
- 现象：翻译后母本（原文）被直接改成目标语言，预合成里的内容却还是原文——「把英文母本变成中文」的错位
- 根因：重写批处理引擎时，翻译阶段误用了 `j.layers`（母本数组），正确应使用 `j.curNewLayers`（本语言复制出的副本数组）；v1 源码翻译的是 `newL`（副本），v2 引擎写成了母本
- 修复：translate 阶段改为遍历并修改副本图层；母本保持原文不动，预合成收走的是已翻译的副本

## [2.0.7] — 2026-08-20

### 修复预合成命名
- 自动预合成的合成名由「语言代码」（EN / CN）改为**语种名称**（英语 / English / 越南语），与输入的目标语言一一对应，不再对不上号
- 查重逻辑同步更新：同时匹配语种名与旧版代码名（兼容历史已生成的代码名合成）

## [2.0.6] — 2026-08-20

### 修复「点击开始处理后无反应」
- 根因：批处理依赖 `app.scheduleTask` 字符串回调驱动，但在 ScriptUI 面板环境中回调可能找不到脚本定义的全局函数（上下文隔离），导致任务永远不推进、按钮变灰、后续点击被静默拦截
- 修复：批处理引擎改为**同步执行**（`__dsJobRunSync`），任务必定推进、必定完成；处理期间界面冻结（无法中途取消），完成后恢复
- 引擎整体加异常保护：单步抛错不会卡死，转为错误提示并正常收尾（结束 undo group）
- 同步处理中「取消」按钮禁用

### 修复查重顺序
- 查重从「处理下一语言前」移到「每个语言开始前」，第一个目标语言现在也会正确查重

## [2.0.5] — 2026-08-20

### 新功能：获取可用模型（参考 DeepSeek Harness 的 Provider 配置）
- 设置 → 模型行新增「获取模型」按钮：调用 API 的模型列表接口并弹出下拉选择
  - OpenAI 兼容协议：`GET {baseUrl}/models`（DeepSeek / OpenAI / Moonshot / GLM / Ollama 均支持）
  - Anthropic 兼容协议：`GET {baseUrl}/v1/models`（端点不支持时会明确提示，仍可手填模型名）
- 模型列表解析原生 JSON 优先 + 正则兜底

## [2.0.4] — 2026-08-20

### API 设置重做（参考标准 Provider 配置表单）
- 新增「API 协议」下拉，写死两种常用协议：
  - OpenAI 兼容：`Authorization: Bearer`，`/chat/completions`（默认地址 `https://api.deepseek.com`）
  - Anthropic 兼容：`x-api-key`，`/v1/messages`（默认地址 `https://api.deepseek.com/anthropic`，`anthropic-version` 头无需发送）
- 移除「预设下拉」——不再写死模型名，模型改为纯文本自由填写（默认 `deepseek-v4-flash`，提示常用模型）
- 切换协议时若未手动改地址，自动切换默认地址
- 请求体与响应解析按协议区分（Anthropic 格式：`max_tokens` + `messages[].content` 字符串、响应取 `content[0].text`）

### 修复矮窗口布局
- 窗口被拉得过矮时底部「开始处理 / 取消」等按钮被裁剪/消失（部分 AE 版本 `minimumSize` 不生效）——`onResize` 强制保持最小高度 540，并压缩提示词输入框最小高度至 60

## [2.0.3] — 2026-08-17

### UI 修复（基于实测反馈）
- 未选中文本层或未输入目标语言时不再显示「0 字符」估算
- 提示词面板改为弹性区域：窗口拉高/拉宽时由提示词输入框吸收多余空间，消除中部大片空隙，同时解决输入框过小的问题（设高度上限 480 防失控）
- 「开始处理 / 取消」按钮高度统一（均为 40）
- 底部「重试失败项 / 导出日志」按钮改为靠右对齐，随窗口宽度变化
- 目标语言标签缩短（格式说明保留在下方小字提示）
- 独立窗口设置最小尺寸 400×560，防止窗口缩得过小导致控件挤压

## [2.0.2] — 2026-08-17

### UI 重构（设置改为弹出对话框）
- 移除常驻的「高级设置」折叠面板——ScriptUI 中隐藏面板仍占用布局空间（假隐藏），导致下方内容无法上递补
- 主界面右上角新增「设置」按钮，API / 请求 / 语言等全部设置移入弹出对话框，主界面不再被设置内容侵占
- 界面语言切换（中文 / English）集成进设置对话框，从主界面标题栏移除（不再显眼）
- 删除主界面内的标题行，消除「DeepSeek 翻译器」文字重复（窗口标题仅保留一处）
- 设置对话框内支持：API 地址 / API Key（可直接编辑与清除）/ 模型名 / 预设下拉 / 测试连接 / 超时 / 重试 / 间隔 / 禁用思考 / 界面语言
- 简化 applyLanguage 与 uiReg 注册表，移除已废弃的 Key 输入对话框与主界面控件

### 工程
- 单元测试维持 69 项断言通过

## [2.0.1] — 2026-08-17

### 修复
- 修复 UI 构建使用未定义变量 `container` 导致脚本无法运行的问题（改为 `thisObj`，支持面板 / 独立窗口两种模式）

### 映射机制通用化（移除「语言名=代码」映射表）
- 删除高级设置中的映射规则编辑框，改为内置 44 种常见语言字典（只读）
- 目标语言输入支持四种形式，逗号 / 顿号分隔：
  - 中文名：`英语`
  - 英文名：`English`
  - 语言代码：`EN` / `cn`（大小写不敏感，自动反查规范名用于提示词）
  - 自定义：`克丘亚语=QU`（名字=代码，字典外语言）
- 未识别语言给出明确提示（提示可输入代码或名字=代码）

### UI / 排版重构
- 面板整体间距与边距规范化（spacing 8 / margins 12），消除控件挤压
- 三个选项复选框由单行并排改为两行布局，避免超宽截断
- 「开始处理 / 取消」改为并排一行；结果区（失败数 + 重试 + 导出）真正归入一行
- 高级设置面板展开/收起去掉 maximumSize 高度 hack，改为纯 visible 切换
- API Key 显示改为短掩码（sk-••••abcd），不再附带长提示文字
- 目标语言输入框下方增加支持格式提示小字
- 独立窗口默认尺寸 440×700，打开即有合理布局
- 移除空 group 占位、为下拉框/输入框设置合适宽度

### 工程
- 单元测试增至 69 项断言（新增语言字典与通用解析用例）

## [2.0.0] — 2026-08-17

### 安全
- 彻底移除硬编码 API Key：Key 改为运行时输入，仅保存在 AE 设置（app.settings）中，脚本文件不再包含任何密钥
- 新增「设置 Key / 修改 / 清除」按钮与掩码显示；README 增加安全说明与旧 Key 轮换提醒

### 兼容性
- 模型预设更新为 DeepSeek V4 官方模型（`deepseek-v4-flash` / `deepseek-v4-pro`），移除已退役的 `deepseek-chat` / `deepseek-reasoner`
- 支持任意 OpenAI 兼容接口：可自定义 API 地址与模型名（OpenAI / Moonshot / 智谱 GLM / Ollama 等）
- `thinking:{"type":"disabled"}` 参数改为可配置（默认开启，翻译场景提速降费）

### 新功能
- 中 / 英双语界面，一键切换（含双语默认映射与提示词）
- 按行翻译模式（保持行数；行数不匹配自动逐行重试）
- 失败自动重试（次数 / 间隔可配置，递增退避）
- 处理中可取消（基于 app.scheduleTask 的异步引擎，UI 实时进度）
- 导出 CSV 翻译日志（语言 / 图层 / 状态 / 原文 / 译文 / 错误），一键重试失败项
- 字符量预估与确认；请求间可配间隔防限流
- 设置自动记忆（v1 的目标语言记忆自动迁移）

### 健壮性
- 临时文件改为「时间戳 + 随机数」唯一命名，消除并发冲突与残留
- 响应解析改为 JSON.parse 优先 + 正则兜底；修复转义序列解码 bug
- 修复 escapeJSONString 中 `\b` 正则误匹配单词边界的问题（退格符改用 `\x08`）
- curl 追加 HTTP 状态码输出，错误信息包含 HTTP code 与服务商返回详情
- 单次调用异常不再中断整批：逐层 try/catch，失败项集中报告与重试

### 工程
- 纯函数核心与 AE 环境解耦，新增 Node 单元测试（`test/core.test.js`，65 项断言）
- 中英双语 README、MIT License、CHANGELOG

## [1.0.0] — 原始版本

- 初版功能：DeepSeek 批量翻译、父子级修复、遮罩完整性检查、查重、自动预合成、undo group
- ⚠️ 注意：v1 中 API Key 明文硬编码在脚本内。若你持有 v1 版本，请停止使用并在 DeepSeek 控制台轮换该 Key。
